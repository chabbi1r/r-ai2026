name: CI - RDP Auto-Restart & Data Sync

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
      actions: write

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Optimize Windows & Set 16GB Virtual RAM
      shell: powershell
      run: |
        # Disable Defender and Updates to free up CPU
        Set-MpPreference -DisableRealtimeMonitoring $true
        Stop-Service -Name wuauserv -Force -ErrorAction SilentlyContinue
        Set-Service -Name wuauserv -StartupType Disabled -ErrorAction SilentlyContinue
        
        # Allocate 16GB (16384 MB) of Disk Space as Virtual RAM (Pagefile)
        $PageFile = "C:\pagefile.sys"
        $SizeMB = 32000
        Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Session Manager\Memory Management" -Name "PagingFiles" -Value "$PageFile $SizeMB $SizeMB"
        Write-Host "16GB Virtual RAM successfully allocated!"

    - name: Restore Saved Data
      shell: bash
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        mkdir -p /c/MySavedData
        
        # Try to clone the data branch
        git clone --branch data-storage https://${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}.git storage_temp || echo "No previous data found. Starting fresh."
        
        # If data exists, copy it to C:\MySavedData
        if [ -d "storage_temp/MySavedData" ]; then
            cp -r storage_temp/MySavedData/* /c/MySavedData/ || true
            echo "Data restored successfully to C:\MySavedData"
        fi

    - name: Configure RDP & User
      shell: powershell
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
        $password = ConvertTo-SecureString "P@ssw0rd!" -AsPlainText -Force
        New-LocalUser -Name "runneradmin" -Password $password -FullName "Runner Admin" -ErrorAction SilentlyContinue | Out-Null
        Set-LocalUser -Name "runneradmin" -Password $password -ErrorAction SilentlyContinue | Out-Null

    - name: Download & Extract Bore
      shell: powershell
      run: |
        Invoke-WebRequest https://github.com/ekzhang/bore/releases/download/v0.5.1/bore-v0.5.1-x86_64-pc-windows-msvc.zip -OutFile bore.zip
        Expand-Archive bore.zip -DestinationPath .

    - name: Start Bore Tunnel (Background Loop)
      shell: powershell
      run: |
        $script = @"
        while (`$true) {
            .\bore.exe local 3389 --to bore.pub 2>&1 | Out-File -FilePath bore_log.txt -Encoding utf8 -Append
            Start-Sleep -Seconds 5
        }
        "@
        Set-Content -Path "run_bore.ps1" -Value $script
        Start-Process powershell -ArgumentList "-File run_bore.ps1" -WindowStyle Hidden

    - name: Keep-alive, Monitor Tunnel & Auto-Save
      shell: bash
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        echo "Waiting 15 seconds for Bore to connect..."
        sleep 15
        
        last_port=""
        end_time=$((SECONDS + 20700)) # 5 hours and 45 minutes
        
        echo "Server is running. Monitoring for connection changes..."
        
        while [ $SECONDS -lt $end_time ]; do
          current_port=$(grep "listening at" bore_log.txt | tail -n 1)
          if [ "$current_port" != "$last_port" ] && [ ! -z "$current_port" ]; then
            echo "=================================================="
            echo "🟢 RDP CONNECTION INFO (UPDATED):"
            echo "$current_port"
            echo "Username: runneradmin"
            echo "Password: P@ssw0rd!"
            echo "Save your files inside C:\MySavedData"
            echo "=================================================="
            last_port=$current_port
          fi
          sleep 10
        done
        
        echo "Time's up! Saving your data from C:\MySavedData to GitHub..."
        mkdir -p sync_folder/MySavedData
        cp -r /c/MySavedData/* sync_folder/MySavedData/ || true
        cd sync_folder
        git init
        git remote add origin https://${GH_TOKEN}@github.com/${{ github.repository }}.git
        git checkout -b data-storage
        git add .
        git commit -m "Auto-save data - $(date)"
        git push origin data-storage --force
        
        echo "Triggering next RDP session..."
        gh workflow run "${{ github.workflow }}"
        
        echo "Closing current session in 5 minutes..."
        sleep 300
